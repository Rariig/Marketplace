// EditPlay.js

const editPlay = (play) => {
    // For now, we are just logging the play, but you can open a modal or a form
    console.log('Edit play:', play);
    // You could use a modal or redirect to an edit form with the play data pre-filled
  };
  
  export default editPlay;
  